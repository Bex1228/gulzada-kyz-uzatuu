import{useRef,useState}from'react';import HeroSection from'./components/HeroSection';import InvitationSection from'./components/InvitationSection';import PoemSection from'./components/PoemSection';import HostsSection from'./components/HostsSection';import CalendarSection from'./components/CalendarSection';import VenueSection from'./components/VenueSection';import CountdownSection from'./components/CountdownSection';import FooterSection from'./components/FooterSection';import MusicButton from'./components/MusicButton';export default function App(){const audio=useRef<HTMLAudioElement>(null);const[playing,setPlaying]=useState(false);const[opened,setOpened]=useState(false);async function toggle(){if(!audio.current)return;if(playing){audio.current.pause();setPlaying(false)}else{try{await audio.current.play();setPlaying(true)}catch{setPlaying(false)}}}async function open(){if(opened)return;setOpened(true);try{await audio.current?.play();setPlaying(true)}catch{}setTimeout(()=>document.getElementById('invitation')?.scrollIntoView({behavior:'smooth'}),1250)}return <main className="invitationShell"><audio ref={audio} src="/music/music.mp3" loop onEnded={()=>setPlaying(false)}/><MusicButton playing={playing} onClick={toggle}/><HeroSection opened={opened} onOpen={open}/><InvitationSection/><PoemSection/><HostsSection/><CalendarSection/><VenueSection/><CountdownSection/><FooterSection/></main>}
